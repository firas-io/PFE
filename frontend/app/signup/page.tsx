import { SignupForm } from './_components/SignupForm';

export const metadata = {
  title: "Créer un compte | HabitFlow",
};

const SignupPage = async () => <SignupForm />;

export default SignupPage;
